/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ExerciseService } from "../exercise.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ExerciseCreateInput } from "./ExerciseCreateInput";
import { Exercise } from "./Exercise";
import { ExerciseFindManyArgs } from "./ExerciseFindManyArgs";
import { ExerciseWhereUniqueInput } from "./ExerciseWhereUniqueInput";
import { ExerciseUpdateInput } from "./ExerciseUpdateInput";
import { ExerciseSessionFindManyArgs } from "../../exerciseSession/base/ExerciseSessionFindManyArgs";
import { ExerciseSession } from "../../exerciseSession/base/ExerciseSession";
import { ExerciseSessionWhereUniqueInput } from "../../exerciseSession/base/ExerciseSessionWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ExerciseControllerBase {
  constructor(
    protected readonly service: ExerciseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Exercise })
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createExercise(
    @common.Body() data: ExerciseCreateInput
  ): Promise<Exercise> {
    return await this.service.createExercise({
      data: data,
      select: {
        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Exercise] })
  @ApiNestedQuery(ExerciseFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async exercises(@common.Req() request: Request): Promise<Exercise[]> {
    const args = plainToClass(ExerciseFindManyArgs, request.query);
    return this.service.exercises({
      ...args,
      select: {
        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Exercise })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async exercise(
    @common.Param() params: ExerciseWhereUniqueInput
  ): Promise<Exercise | null> {
    const result = await this.service.exercise({
      where: params,
      select: {
        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Exercise })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateExercise(
    @common.Param() params: ExerciseWhereUniqueInput,
    @common.Body() data: ExerciseUpdateInput
  ): Promise<Exercise | null> {
    try {
      return await this.service.updateExercise({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Exercise })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteExercise(
    @common.Param() params: ExerciseWhereUniqueInput
  ): Promise<Exercise | null> {
    try {
      return await this.service.deleteExercise({
        where: params,
        select: {
          createdAt: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/exerciseSessions")
  @ApiNestedQuery(ExerciseSessionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ExerciseSession",
    action: "read",
    possession: "any",
  })
  async findExerciseSessions(
    @common.Req() request: Request,
    @common.Param() params: ExerciseWhereUniqueInput
  ): Promise<ExerciseSession[]> {
    const query = plainToClass(ExerciseSessionFindManyArgs, request.query);
    const results = await this.service.findExerciseSessions(params.id, {
      ...query,
      select: {
        createdAt: true,
        endTime: true,

        exercise: {
          select: {
            id: true,
          },
        },

        id: true,
        startTime: true,
        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/exerciseSessions")
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "update",
    possession: "any",
  })
  async connectExerciseSessions(
    @common.Param() params: ExerciseWhereUniqueInput,
    @common.Body() body: ExerciseSessionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      exerciseSessions: {
        connect: body,
      },
    };
    await this.service.updateExercise({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/exerciseSessions")
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "update",
    possession: "any",
  })
  async updateExerciseSessions(
    @common.Param() params: ExerciseWhereUniqueInput,
    @common.Body() body: ExerciseSessionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      exerciseSessions: {
        set: body,
      },
    };
    await this.service.updateExercise({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/exerciseSessions")
  @nestAccessControl.UseRoles({
    resource: "Exercise",
    action: "update",
    possession: "any",
  })
  async disconnectExerciseSessions(
    @common.Param() params: ExerciseWhereUniqueInput,
    @common.Body() body: ExerciseSessionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      exerciseSessions: {
        disconnect: body,
      },
    };
    await this.service.updateExercise({
      where: params,
      data,
      select: { id: true },
    });
  }
}
